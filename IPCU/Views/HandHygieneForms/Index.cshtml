@model IEnumerable<IPCU.Models.HandHygieneForm>
@{
    ViewData["Title"] = "Hand Hygiene Forms";
}

<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1>Hand Hygiene Forms</h1>
        <a asp-action="Create" class="btn btn-primary">
            <i class="bi bi-plus-circle"></i> Create New Form
        </a>
    </div>

    @if (!Model.Any())
    {
        <div class="alert alert-info">
            No hand hygiene forms found. Click the "Create New Form" button to add one.
        </div>
    }
    else
    {
        <div class="table-responsive">
            <table class="table table-striped table-hover">
                <thead class="table-dark">
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.HHId)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Area)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Observer)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Date)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Name)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.ComplianceRate)
                        </th>
                        <th>Activities</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.HHId)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Area)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Observer)
                            </td>
                            <td>
                                @item.Date.ToString("MM/dd/yyyy")
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Name)
                            </td>
                            <td>
                                @item.ComplianceRate.ToString("P1")
                            </td>
                            <td>
                                <button class="btn btn-sm btn-outline-info" type="button"
                                        data-bs-toggle="collapse"
                                        data-bs-target="#activities-@item.HHId"
                                        aria-expanded="false"
                                        aria-controls="activities-@item.HHId">
                                    Show @(item.Activities?.Count ?? 0) Activities
                                </button>
                            </td>
                            <td>
                                <div class="btn-group" role="group">
                                    <a asp-action="Edit" asp-route-id="@item.HHId" class="btn btn-sm btn-outline-primary">Edit</a>
                                    <a asp-action="Details" asp-route-id="@item.HHId" class="btn btn-sm btn-outline-secondary">Details</a>
                                    <a asp-action="Delete" asp-route-id="@item.HHId" class="btn btn-sm btn-outline-danger">Delete</a>
                                </div>
                            </td>
                        </tr>
                        <tr>
                            <td colspan="8" class="p-0">
                                <div class="collapse" id="activities-@item.HHId">
                                    <div class="card card-body m-2">
                                        @if (item.Activities != null && item.Activities.Any())
                                        {
                                            <h6 class="mb-3">Activities for Form #@item.HHId</h6>
                                            <div class="table-responsive">
                                                <table class="table table-sm table-bordered">
                                                    <thead class="table-light">
                                                        <tr>
                                                            <th>Activity</th>
                                                            <th>Before Hand Rub</th>
                                                            <th>Before Hand Wash</th>
                                                            <th>After Hand Rub</th>
                                                            <th>After Hand Wash</th>
                                                            <th>Gloves</th>
                                                        </tr>
                                                    </thead>
                                                    <tbody>
                                                        @foreach (var activity in item.Activities)
                                                        {
                                                            <tr>
                                                                <td>@activity.Activity</td>
                                                                <td>@activity.BeforeHandRub</td>
                                                                <td>@activity.BeforeHandWash</td>
                                                                <td>@activity.AfterHandRub</td>
                                                                <td>@activity.AfterHandWash</td>
                                                                <td>
                                                                    @if (activity.Gloves == "True")
                                                                    {
                                                                        <span class="text-success">Yes</span>
                                                                    }
                                                                    else
                                                                    {
                                                                        <span class="text-danger">No</span>
                                                                    }
                                                                </td>
                                                            </tr>
                                                        }
                                                    </tbody>
                                                </table>
                                            </div>
                                        }
                                        else
                                        {
                                            <p class="text-muted">No activities found for this form.</p>
                                        }
                                        <div class="text-end mt-2">
                                            <a asp-action="AddActivities" asp-route-id="@item.HHId" class="btn btn-sm btn-outline-success">
                                                Add More Activities
                                            </a>
                                        </div>
                                    </div>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
</div>

@section Scripts {
    <script>
        // Optional: Add JavaScript for additional interactivity
        $(document).ready(function() {
            // Highlight rows on hover
            $('tbody tr').hover(
                function() { $(this).addClass('table-active'); },
                function() { $(this).removeClass('table-active'); }
            );
        });
    </script>
}